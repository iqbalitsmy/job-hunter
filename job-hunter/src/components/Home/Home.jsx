import React from 'react';
import Header from '../Header/Header';

const Home = () => {
    return (
        <header className='container mx-auto'>
            <Header />
        </header>
    );
};

export default Home;